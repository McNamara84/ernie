# This directory contains self-signed SSL certificates for local development.
# Certificates are generated using docker/generate-certs.ps1 (Windows) or docker/generate-certs.sh (Unix).
# These files are ignored by git for security reasons.
